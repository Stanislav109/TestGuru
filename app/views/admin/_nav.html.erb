<nav class="navbar navbar-light bg-primary row">
  <div class="container">
    <span class="navbar-brand">
      <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>
      <%= link_to 'All Gists', admin_gists_path, class: "btn btn-sm btn-secondary" %>
    </span>
    
    <span class="navbar-brand">
      <%= link_to t('.adminpanel'), admin_tests_path, class: 'navbar-brand' %>
    </span>

    <span class="navbar-text">
      <% if user_signed_in? %>
        Welcome, <b><%= current_user.email %></b> Guru |
        <%= link_to link_to t('.logout'), destroy_user_session_path, method: :delete, class: "btn btn-sm btn-primary" %>
      <% else %>
        <%= link_to t('.login'), new_session_path(resource_name) %> |
        <%= link_to t('.signin'), new_registration_path(resource_name) %>
      <% end %>
    </span>
  </div>
</nav>  